# results/

Small outputs that count as evidence: JSON, JSONL, CSV, Markdown, plots.

Do not store credentials, signed URLs, machine identifiers, or large
raw logs here. Large binaries go to `artifacts/` (gitignored).
