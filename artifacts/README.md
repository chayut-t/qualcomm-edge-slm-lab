# artifacts/

Local models, ONNX graphs, encodings, QNN context binaries, and device
packages live here. Git ignores everything in this folder except this
README. Never commit model weights or compiled bundles.
