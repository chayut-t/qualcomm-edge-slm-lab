# Copy to .ai-local/secrets/qai-hub.env or .env (both gitignored).
# Never put real values in tracked files.

# Qualcomm AI Hub API token
QAI_HUB_API_TOKEN=

# Hugging Face access token (only if a gated model needs it)
HF_TOKEN=
